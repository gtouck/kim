# key-input-monitor Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-03-06

## Active Technologies

- Rust stable (≥ 1.75) + `windows = "0.56"` (windows-rs，WinAPI 绑定) · `rusqlite = "0.31"` (bundled) · `crossbeam-channel = "0.5"` · `clap = "4"` (derive) · `chrono = "0.4"` · `log + simplelog = "0.12"` (001-input-stats)

## Project Structure

```text
src/
tests/
```

## Commands

cargo test; cargo clippy

## Code Style

Rust stable (≥ 1.75): Follow standard conventions

## Recent Changes

- 001-input-stats: Added Rust stable (≥ 1.75) + `windows = "0.56"` (windows-rs，WinAPI 绑定) · `rusqlite = "0.31"` (bundled) · `crossbeam-channel = "0.5"` · `clap = "4"` (derive) · `chrono = "0.4"` · `log + simplelog = "0.12"`

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
