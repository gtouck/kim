// kim apps: per-app stats table rendering
// Implementation: Phase 7 (T043) and Phase 9 (T054)

/// Stub — implemented in Phase 7 (T043).
pub fn cmd_apps(conn: &rusqlite::Connection, _date: Option<&str>, _top: u32) -> i32 {
    let _ = conn;
    eprintln!("kim apps is not yet available (coming in Phase 7).");
    1
}
